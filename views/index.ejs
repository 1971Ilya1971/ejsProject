<%- include("layout/header") %>
  <main>
    <h1>Home page</h1>

    <form action="/" method="post">
       <input type="text" placeholder="Enter data" name="plName">
       <button type="submit">Add data</button>
    </form>

    <ul>
      <% if(plNames.length>0){ %>

        <% for(let i=0; i<plNames.length; i++){ %>
          <li><%=plNames[i]%></li>
        <% } %>

      <% }else{ %>
        <p>No data anaviable</p>    
      <% } %>  
    </ul>
  </main> 
   
<%- include("layout/footer") %>
